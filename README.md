# UDPSockets
C++ client-server application developed for a Computer Networks project. Implements socket-based communication where a server handles multiple clients, processes requests, manages file access, and enforces permissions (read, write, execute).
